import { AUTO_FILL_POSTS, AUTO_FILL_DATA, AUTO_LOGIN } from '../action-types';

export const autoFillPosts = () => ({ type: AUTO_FILL_POSTS });
export const autoFillData = () => ({ type: AUTO_FILL_DATA });
export const autoLoginAction = () => ({ type: AUTO_LOGIN });
